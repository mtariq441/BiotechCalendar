# ==============================================
# LOCAL DEVELOPMENT (Replit)
# ==============================================
# When running locally on Replit, these values are automatically provided:
# - DATABASE_URL (from Replit PostgreSQL database)
# - REPL_ID (for Replit Auth)
# - ISSUER_URL (for Replit Auth)
#
# You only need to add:
# - SESSION_SECRET (generate a random string)
# - OPENAI_API_KEY (optional, for AI features)
# ==============================================

# Session Secret (Required for local development)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-local-development-session-secret-here

# OpenAI API Key (Optional - enables AI analysis features)
# OPENAI_API_KEY=sk-your-openai-api-key-here

# Local Environment (automatically set by Replit)
# NODE_ENV=development
# DATABASE_URL=<provided by Replit>
# ISSUER_URL=https://replit.com/oidc
# REPL_ID=<provided by Replit>
# PORT=5000

# ==============================================
# VERCEL DEPLOYMENT
# ==============================================
# For Vercel deployment, see .env.vercel file for required variables
# All Vercel environment variables should be added through the Vercel dashboard
# at Settings > Environment Variables
# ==============================================
