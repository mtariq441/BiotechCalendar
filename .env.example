# Database Configuration
# Use either DATABASE_URL (direct PostgreSQL) OR Supabase credentials

# Option 1: Direct PostgreSQL (Recommended for Vercel)
DATABASE_URL=postgresql://user:password@host:5432/database

# Option 2: Supabase (Alternative)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key

# Session Secret (Required - generate a random 32+ character string)
SESSION_SECRET=your-secret-key-minimum-32-characters-long

# Authentication (Required for Replit Auth, Optional for Vercel)
# For Replit deployment
ISSUER_URL=https://replit.com/oidc
REPL_ID=your-repl-id

# For Vercel deployment with custom OAuth
# ISSUER_URL=https://your-auth-provider.com/oidc
# REPL_ID=your-oauth-client-id

# OpenAI API Key (Optional - for AI analysis features)
# OPENAI_API_KEY=sk-...

# Node Environment
NODE_ENV=development
